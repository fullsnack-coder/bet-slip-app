import { createTheme } from "@mui/material";

const appTheme = createTheme({
  palette: {
    primary: {
        main: "#65a30d"
    },
    secondary: {
        main: '#222222'
    }
  },
});

export default appTheme;
